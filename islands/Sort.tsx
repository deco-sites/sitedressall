import type { Props } from "../components/search/Sort.tsx";
import Component from "../components/search/Sort.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
